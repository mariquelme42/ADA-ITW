// Ejercicio 6 clase 10: Declara dos variables con valores numéricos. Utiliza operadores de comparación y lógicos para verificar si ambos números son mayores que 10 y muestra el resultado en la consola. Utiliza los operadores mayor que (>) y AND (&&).

// Declarar y asignar valor numérico a dos variables
let num1 = 12;
let num2 = 11;

// Verificar si ambos números son mayores que 10
let resultado = (num1 > 10) && (num2 > 10);

// Mostrar el resultado en la consola
console.log("¿Ambos números son mayores que 10?", resultado);